# 💾 Бекапы (демон)

## 📌 Описание

Исполнительный модуль бекапов на ноде: создание, восстановление и ротация бекапов приложений. Политики и расписания может задавать платформа ([💾 features/backups](https://github.com/AdminServiceCloud/asc-platform/blob/main/docs/features/backups.md)), но модуль полностью работает и автономно через CLI (`asc backup ...`) — аккаунт платформы не нужен.

## 🎯 Сценарии использования

- `asc backup create <app>` — бекап приложения в настроенные хранилища (по умолчанию `local`), либо конкретное — `--storage <имя>` (можно несколько раз).
- `asc backup restore <app> <backup-id>` — восстановление; приложение должно быть остановлено (деструктивно: заменяет repository, config и data).
- `asc backup list <app> [--storage <имя>]` — бекапы приложения на одном хранилище, от старых к новым.
- `asc backup prune <app> --keep <n> [--storage <имя>]` — вручную удалить старые бекапы сверх `n` (ротация также выполняется автоматически после каждого `create` — по настройке `keep` самого приложения).
- `asc backup storage add|list|remove` — управление тем, куда идут бекапы; `asc app settings <app>` (категория `backups`) — в какие из этих хранилищ бекапится конкретное приложение, сколько копий хранить и с какой периодичностью (расписание исполняет демон, см. [⏰ scheduler](/ru/cli/scheduler)).

## 🏗️ Техническое решение

### Что входит в бекап

Архив (`tar.gz`) подкаталогов `repository/`, `config/` и `data/` каталога приложения — всё, кроме `meta.json` (он пересоздаётся при восстановлении, как и при [🧬 клонировании](/ru/cli/app-management)). `asc.backup.yaml` в корне репозитория пакета исключает пути из архива:

```yaml
exclude:
  - data/cache/**
  - repository/vendor
```

Паттерны — относительно каталога приложения, поддерживают `*` (любая последовательность символов в пределах одного сегмента пути), `**` (любая последовательность, включая `/`) и `?` (один символ); исключение каталога исключает и всё, что внутри — как в `.gitignore`. Файловый бекап не заменяет консистентность на уровне приложения (например, дамп БД) — pre/post-хуки бекапа — более поздний инкремент.

**Выбор файлов для одного бекапа (DMN-118).** `CreateBackupRequest` (REST: `POST /v1/apps/{id}/backups`) и задача расписания `ScheduleBackupJob` принимают необязательные списки `include` и `exclude` в том же синтаксисе. Непустой `include` сужает архив до совпавших файлов (шаблон каталога берёт всё внутри; каталоги всё равно обходятся, поэтому `**/*.db` находит файлы на любой глубине); `exclude` применяется поверх него и поверх `asc.backup.yaml` — исключение всегда сильнее. Шаблоны обрезаются по краям, завершающий `/` отбрасывается; абсолютные пути, сегменты `..` и обратные слеши отклоняются с `InvalidArgument`, в каждом списке — не больше 64 шаблонов. Демон, который не знает этих полей, игнорирует их и бекапит всё.

### Хранилища (трейт `BackupStorage`, `src/daemon/backup/storage.rs`)

- **`local`** — всегда доступно, без настройки: обычный каталог (`<data_dir>/backups`, по умолчанию `/var/lib/asc/backups`). Единственный тип хранилища, который сегодня реально что-то переносит.
- **`s3`** — S3-совместимое хранилище (AWS S3, MinIO, Backblaze B2, Wasabi, Yandex Object Storage…), DMN-115: `asc backup storage add <имя> --type s3 --bucket … --region … [--endpoint …] --access-key … --secret-key … [--prefix …]`. Настоящий перенос (`src/daemon/backup/s3.rs`): подпись Signature V4 вручную (HMAC-SHA256 поверх уже используемого `sha2`), блокирующий HTTPS-клиент `ureq` на том же rustls/ring, что и API демона. Архивы до 64 МиБ — одним `PUT`, крупнее — multipart (части от 16 МиБ, при сбое незавершённая загрузка отменяется); тело загрузки передаётся как `UNSIGNED-PAYLOAD` (архив не читается дважды), остальные запросы подписывают хеш тела. Адресация — virtual-hosted для AWS, path-style для остальных (тот же выбор, что у проверки подключения платформы); листинг — `ListObjectsV2` с пагинацией, в выдачу попадают только архивы ровно этого приложения (`demo` не подхватывает `demo-2`). Ошибки несут код S3 (`SignatureDoesNotMatch`, `NoSuchBucket`…), но никогда — реквизиты.
- **`ftp` / `sftp`** — настраиваются так же (параметры подключения хранятся как источники реестра — системный список `/etc/asc/backup-storages.toml`, управляется root и виден всем, плюс пользовательский `~/.config/asc/backup-storages.toml`; файл 0600, так как содержит учётные данные), но перенос пока не реализован — любая операция возвращает понятную ошибку «не реализовано».
- `managed_by` у записи хранилища: платформа кладёт свои S3-хранилища организации как `platform-<id>` с `managed_by = "platform"` (префикс ключей `<path_prefix>/asc-backups/<id ноды>`) и может их обновлять; запись оператора платформа не заменит, и наоборот.
- Имя настроенного хранилища не может быть `local` (зарезервировано), обычный пользователь не может затенить или удалить системное хранилище — те же правила, что и у [📦 источников реестра](/ru/cli/package-manager).

### Политика бекапов (`asc app settings` → `backups`)

Хранится под зарезервированным ключом `$backup` в `config/settings.json`, рядом с `$quota`/`$start_command` (та же конвенция, DMN-017/030): `storages` (множественный выбор, переключается по номеру в редакторе), `keep` (сколько копий хранить на каждое хранилище — обрезается автоматически сразу после каждого `create`), `schedule` (`daily@HH:MM`, просто `HH:MM` или cron-выражение из пяти полей `мин час день месяц день_недели`; редактор проверяет синтаксис). **`schedule` исполняется планировщиком демона** ([⏰ scheduler](/ru/cli/scheduler), DMN-012): раз в минуту он сверяет политики всех приложений с локальным временем ноды и запускает подошедшие бекапы в хранилища политики с её ротацией `keep` — демон должен быть запущен (`asc service install` или `asc serve`). `asc backup create <app>` без `--storage` использует хранилища из политики, а при пустой политике — только `local`.

### Листинг и API (`BackupService`, capability `backups`)

Листинг отдаёт имя архива, хранилище, размер и время создания (из имени `<app>-<unix-ts>.tar.gz`); `asc backup list` печатает имя и размер. Бекап в несколько хранилищ собирает архив **один раз** и отправляет тот же файл в каждое — все копии одного снимка; результат — по каждому хранилищу. Имена архивов на восстановление и удаление проверяются (`validate_backup_name`): путь с `/` или архив другого приложения отклоняются до того, как имя попадёт в путь или ключ объекта.

API: `ListBackupStorages`, `UpsertBackupStorage` (S3 или локальный каталог, с `managed_by`), `RemoveBackupStorage`, `ListBackups` (одно приложение или все, одно хранилище или все; недоступное хранилище — в `errors`, а не ошибка вызова), `CreateBackup`, `RestoreBackup` (`stop_app` — остановить, восстановить и запустить обратно), `DeleteBackup`. REST-зеркало: `GET /v1/backups`, `GET /v1/backups/storages`, `PUT/DELETE /v1/backups/storages/{name}`, `POST /v1/apps/{id}/backups`, `POST /v1/apps/{id}/backups/restore`, `DELETE /v1/apps/{id}/backups/{storage}/{name}` ([🔌 api](/ru/cli/api)). Этим пользуется платформа ([💾 features/backups](https://github.com/AdminServiceCloud/asc-platform/blob/main/docs/features/backups.md)).

### Восстановление

Скачивает архив во временный файл, затем **полностью заменяет** `repository/`, `config/` и `data/` каталога приложения (удаляются, затем распаковываются заново) — результат в точности соответствует бекапу, а не слиянию с тем, что было раньше. CLI отказывается восстанавливать поверх запущенного приложения.

## 🔗 Связанные задачи

DMN-009, DMN-012, DMN-115, DMN-118, NODE-049, BE-005 в [ROADMAP.md](https://github.com/AdminServiceCloud/asc-platform/blob/main/ROADMAP.md).
